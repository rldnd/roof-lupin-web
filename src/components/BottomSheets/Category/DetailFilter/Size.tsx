"use client";

import styles from "./size.module.scss";

const Size: React.FC = () => {
  return (
    <section className={styles.wrapper}>
      <h2>평수</h2>
    </section>
  );
};

export default Size;
