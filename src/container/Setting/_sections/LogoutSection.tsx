"use client";

import ButtonItem from "../ButtonItem";
import Section from "../Section";

const LogoutSection: React.FC = () => {
  return (
    <Section>
      <ButtonItem onClick={() => {}}>로그아웃</ButtonItem>
    </Section>
  );
};

export default LogoutSection;
