"use client";

import type { Review } from "@/common/types/review";

import styles from "./carousel.module.scss";

interface Props {
  bestReviews: Review[];
}

const Carousel: React.FC<Props> = ({ bestReviews }) => {
  return <></>;
};

export default Carousel;
